@model IEnumerable<Asignacion.Entidades.Aula>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.numaula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.idsede)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.numaula)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.idsede)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idaula">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idaula">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idaula">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
